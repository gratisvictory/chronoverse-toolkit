# Rule 3: Prefix Types With `T` and Interfaces With `I`

Always prefix **types** with `T` and **interfaces** with `I`.

This applies to all TypeScript files, including **JSX and TSX**.
Do not declare types or interfaces without these prefixes.

This convention prevents naming conflicts, improves readability, and makes type-level entities immediately recognizable.
