{
  "name": "chronoverse-toolkit",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/**/*"
  ],
  "scripts": {
    "build": "nx run-many -t build",
    "check-types": "nx run-many -t check-types",
    "clean": "nx run-many -t clean",
    "clean:all": "nx run-many -t clean:all",
    "format:check": "nx run-many -t format:check",
    "format:check:all": "prettier -c .",
    "format:check:cache": "nx run-many -t format:check:cache",
    "format:check:cache:all": "prettier -c . --cache",
    "format:fix": "nx run-many -t format:fix",
    "format:fix:all": "prettier -w . --log-level=warn",
    "format:fix:cache": "nx run-many -t format:fix:cache",
    "format:fix:cache:all": "prettier -w . --cache --log-level=warn",
    "lint:check": "nx run-many -t lint:check",
    "lint:check:all": "eslint -c ./eslint.config.ts",
    "lint:check:cache": "eslint -c ./eslint.config.js --cache --cache-location=node_modules/.cache/.eslintcache",
    "lint:check:cache:all": "nx run-many -t lint:check:cache",
    "lint:fix:all": "nx run-many -t lint:fix",
    "lint:fix:cache": "eslint -c ./eslint.config.js --cache --cache-location=node_modules/.cache/.eslintcache",
    "lint:inspect": "yarn dlx @eslint/config-inspector@latest",
    "lint:ws": "yarn dlx sherif@latest",
    "prepare": "husky"
  },
  "devDependencies": {
    "@chronoverse-toolkit/commitlint": "workspace:^",
    "@chronoverse-toolkit/eslint-typescript": "workspace:^",
    "@chronoverse-toolkit/flat": "workspace:^",
    "@chronoverse-toolkit/prettier": "workspace:^",
    "@nx/js": "^22.3.3",
    "@types/node": "^24.10.4",
    "eslint": "^9.39.2",
    "husky": "^9.1.7",
    "jiti": "^2.6.1",
    "nx": "22.3.3",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3"
  },
  "packageManager": "yarn@4.12.0"
}
